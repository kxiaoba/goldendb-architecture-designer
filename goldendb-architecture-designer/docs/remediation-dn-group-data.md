# S01b3b2b2b2 逐Group容量与磁盘联动

## 计划

- [x] 可选当前逻辑数据JSON数组，按G1开始；数量等于最终Group数，合计等于租户当前数据量。
- [x] 数据按既有规划期增长倍数增长一次；同Group所有主从副本容量相同。
- [x] 共用逐Group容量函数，联动中心需求、物理主机落位水位、资源明细、Excel。
- [x] 最大Group容量替代倾斜假设的单分片检查，不再次乘数据倾斜倍数。
- [x] 专项/原功能回归、实际导出验证、前后快照和续接记录。

## 范围和限制

原空值保持均匀模型。输入清单描述现有分片，改变Group数后必须重新提供清单，不能自动重分片或平均分摊旧Group。
0表示空Group，允许；负数、字符串、空数组、数量/总量不匹配和非有限数必须拒绝。合计容差为max(1e-9TB,总量×1e-9)，仅用于浮点误差。
这是客户录入数据，不能标为已验证实测；不提供逐Group TPS证据，不修改CN或TPS标定规则。
采用清单时磁盘装箱会与旧平均模型不同，不能越过资源水位、目标中心或反亲和兜底。
磁盘仍沿用原逻辑数据容量口径及预留，未包含RAID净容量、日志、索引/压缩差异、临时空间等S02待办；本批不宣称磁盘规划完全正确或达到生产认证。

## 所有续接项

1. 逐Group性能数据及客户IOPS/网络/日志回放/DN-GTM瓶颈证据。
2. S01b3b2c：版本、同步仲裁、复制延迟、RPO/RTO故障演练证据。
3. S02：RAID和日志临时空间、三年增长及磁盘80%独立水位。
4. S03：行业生产/POC矩阵、原生Excel/WPS验收。
5. 原R01-R18未关闭项保留，见总进度，不因本批关闭。

保存目录 `../../outputs/goldendb-spec-remediation-20260919/`；本批子目录 `s01b3b2b2b2/`。
本批仅本地实施，保留上批未提交修改，不提交发布。

## 2026-09-19 验收

- 新专项30/30；数据倾斜24、热点24、宿主机压力19、单副本21、主分布61、在线跑批45，共224项通过。
- 验证录入[1.5,0.5,0.5,0.5]TB、规划数据增长2倍后为[3,1,1,1]TB；每个中心一份完整副本时各6TB，跨两个中心合计12TB。
- 同机G1+G2占4TB，4TB机型预留20%后不可容纳；跨租户各自Group容量求和，不串租户。
- 非法JSON、非数值、负值、数量/总量不匹配拒绝；空Group允许；清空输入恢复原落位模型。输入不足时停止Excel，修正后恢复。
- 实际Excel1652单元格一致、ZIP/XML通过；未执行原生Excel/WPS验收。邻接回归包含PNG与Excel下载。
- 390/875/1600宽度检查通过，查看390截图无新增遮挡；浏览器异常0；语法/diff检查通过。
- 初次专项缺少既有生产部署策略字段导致测试报错，补齐夹具后通过，未修改业务规则以绕过测试。
- 快照 `before-s01b3b2b2b2.tar.gz` / `after-s01b3b2b2b2.tar.gz`；回归证据 `s01b3b2b2b2-regression/`。

下次优先继续逐Group性能与实测瓶颈证据，再S01b3b2c、S02、S03。不能重做已完成输入，也不能把客户录入视为已核验数据。当前远端仍为002fb24，本批未提交发布。
