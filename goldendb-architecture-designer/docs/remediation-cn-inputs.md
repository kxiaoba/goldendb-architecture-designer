# S01b2b 在线/跑批输入与 CN 标定

2026-09-16 用户确认执行。接续 S01b2a，不覆盖其未提交修改。

## 计划与边界

- [x] 在线 QPS 移入在线 CN，保留唯一 qps 字段及单场景兼容。
- [x] 跑批事务 SQL 平均次数独立，新参数不预填 20；旧值保留。
- [x] 增加 SQL 执行总次数/有效窗口输入，与业务处理量区分。
- [x] 在线/跑批各自支持每核标定、整实例实测折算，记录来源、内存及水位口径。
- [x] 明确小数倍数、比例、整数输入边界，缺少证据与跨标定规格保留风险。
- [x] 测试单 AZ、旧模式、多租户、手动数量、混部、红线、两图、PNG/Excel及响应式。

不修改 DN/GTM/管理节点公式及装箱规则。不把线性折算当作性能保证。
每生产 AZ 独立满足完整场景目标，不按 AZ 数均摊。整实例吞吐/核数只是该 SQL 模型下平均每核能力；已含水位不再折减。
SQL 总次数/秒得到平均 QPS，不是峰值；业务行数、SQL 模板数不能直接用作执行次数。
新字段只在对应模式验证；切换保留原值，隐藏参数不拦截当前模式。

## 存档

修改前：`../../outputs/goldendb-spec-remediation-20260916/before-s01b2b.tar.gz`。
修改后：`../../outputs/goldendb-spec-remediation-20260916/after-s01b2b.tar.gz`。
本次确认范围已实施及验证；未提交、未发布。原 S01b3/S02/S03 与审查未关闭项保持待办。

## 验证结果

- 新专项 `tests/remediation-cn-inputs.mjs`：33/33。
- 原在线/跑批 `remediation-workloads.mjs`：45/45。
- 两模块主分布/组网 `remediation-primary-balance.mjs`：61/61。
- 原 DN 标定 `remediation-spec-s01b2.mjs`：20/20。合计159项，无浏览器异常。
- `node --check`、`git diff --check` 通过。
- 实际下载 Excel：1309单元格逐项匹配，差异0，无公式/错误单元格，ZIP/XML通过。
- 390/875/1600输入区域无横向溢出；已查看手机截图、实际组网PNG和服务器PNG，未见遮挡。
- 证据：上述目录下 `s01b2b/inputs`、`workloads`、`balance`、`dn`；不覆盖上批证据。

## 计算与限制

SQL窗口案例：36000000次 / (2小时 × 3600秒) = 5000平均QPS；独立跑批T=20，得到250TPS。
标定实例16C/64GB、800TPS折算50TPS/核；raw水位0.7、每AZ两实例，安全初算1120TPS。
若输入已含水位560TPS，则折算35TPS/核并使用系数1，仍为1120TPS，不重复折减。
SQL窗口可选故障校验沿用实际有效落位与单机故障检查，工作量单位变为事务，非业务行数。
增长倍数1.5、平均事务SQL次数2.5均已实际输入测试；CPU水位/并行效率1.5、节点数1.5拒绝。

来源只是用户声明，填写不代表验证通过。整实例模式只要规划CPU/内存不同于标定规格就保留跨规格风险。
共享DN/GTM混合负载风险继续保留，没有把在线TPS和跑批TPS机械相加，也没有变更DN公式。
未执行真实Excel/WPS客户端验收及数据库压测；不保证任意规模图像或生产性能。
后续继续S01b3故障容量、S02磁盘/RAID/三年增长、S03行业联调；完整审查计划未全部关闭。
