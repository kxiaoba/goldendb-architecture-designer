# S01b3b2b2a DN 热点压力假设

## 范围与计划

- [x] 增加业务测算逐租户热点倍数，默认1，允许1.5等小数。
- [x] 单副本目标 = 租户规划TPS / Group数 × min(热点倍数, Group数)。
- [x] 单副本、共享宿主机CPU压力、红线、计算过程及Excel使用同一目标。
- [x] 保持自动分片、CN、DN规格、主分布、服务器落位和反推模型不变。
- [x] 专项与邻接回归验证，保存结果、快照。

这是敏感性校验，不是新增性能标定。倍数1仅代表均匀假设，不表示已证明无热点。
单Group最多承担租户总TPS；不修改数据量分布。数据倾斜仍未评估。
宿主机对本机每个不同Group均采用最热Group上界，属于保守包络，不能解释为实际同时发生的负载总和。
缺少TPS或标定的反推场景保持未评估。输入不足1、空值、非有限数不得静默回退。
不调整CN/DN数量和装箱，热点超限要求用户复核规格、分片键和压测，不能承诺增加分片必然消除热点。

## 后续完整计划

1. S01b3b2b2b：逐Group实际负载与数据倾斜；客户实测IOPS、带宽、回放、DN/GTM瓶颈证据。
2. S01b3b2c：版本、同步仲裁、复制延迟、RPO/RTO及故障演练证据。
3. S02：RAID、三年增长、日志临时空间和磁盘80%边界。
4. S03：运营商/银行/政务生产与POC矩阵及客户端导出复核。

既有R01-R18未关闭项不因本批完成而关闭。没有数据库实测，不能用于生产认证。
保存目录：`../../outputs/goldendb-spec-remediation-20260919/`。

## 本次验收与续接

2026-09-19 本地完成，未提交、未推送、未更新Pages。

- 热点专项24/24、宿主机压力19/19、单副本21/21、两模块主分布61/61、在线跑批45/45，共170项通过。
- 专项包含默认兼容、小数、总TPS封顶、单分片、空值/非法输入、真实输入联动、原落位保持、红线与Excel同源。
- 实际下载Excel 1652单元格与导出模型一致，ZIP/XML检查通过；未执行原生Excel/WPS视觉验收。
- 390/875/1600宽度输入布局检查通过，查看390截图未见新增输入遮挡；邻接回归包含两类PNG和Excel。
- 语法检查及diff空白检查通过，浏览器异常0。
- 首次专项反推夹具缺少serverPlan，补齐后继续；只读断言最初在主动修改共享租户参数前取快照，造成假失败，调整为参数准备后、调用审计前取完整模型快照，未放宽断言。
- 修改前/后快照为 `before-s01b3b2b2a.tar.gz` / `after-s01b3b2b2a.tar.gz`。专项证据子目录 `s01b3b2b2a/`，邻接结果分别在 `regression-host/`、`regression-takeover/`、`regression-balance/`、`regression-workloads/`。

后续进展：S01b3b2b2b1已补数据倾斜假设，见 `remediation-dn-data-skew.md`；下次从S01b3b2b2b2逐Group实际数据和磁盘联动继续，勿重复增加热点倍数。默认1只用于兼容旧均匀模型；本批没有厂商性能证明、实际Group负载或真实故障切换验证。
