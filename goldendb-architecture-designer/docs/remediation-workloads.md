# 在线/跑批与实例规格配置

## 批次计划（已批准，实施中）

- [x] W01 保留单场景兼容，增加同租户在线/跑批 CN 分组。
- [x] W02 单 CN/DN 物理核、内存可指定；规格/数量自动与手动联动（工程初算）。
- [x] W03 每个 CN 实例使用自身规格参与中心资源、装箱、水位和反亲和。
- [x] W04 两图、计算说明、红线、PNG/Excel 同源输出。
- [x] W05 本阶段专项与旧功能抽检、真实下载结构核对、存档。
- [ ] W06 完整生产评估扩展与边界复核，见下文；不得视为已验收。

原则：跑批缺少匹配标定不得自动套在线 K；不同工作负载吞吐不可直接相加作 DN 标定。共享 DN 性能需要混合负载等效 TPS 输入或标为未评估。CN 分组不新建租户、DN 数据量或 GTM Group。CPU 线性折算、内存比例仍属工程估算，不是厂商承诺。原单场景输入缺省时保持旧结果。

前序状态：POC 密度批已本地验收，尚未发布。保持所有未提交修改，不覆盖其他文档或证据。完整 R01–R18 遗留仍见 remediation-progress.md。本批未授权提交/发布。

## 2026-09-12 本地验收记录

入口：业务测算的租户编辑区增加“单场景 / 在线与跑批”及 CN/DN 自动或手动规格。在线与跑批分别设置标定、CPU 水位、增长、数量；跑批支持 SQL QPS、数据库 TPS、处理量/有效窗口。缺少跑批标定时不生成有效方案，不套用在线单核值。两场景仍属于同一个租户，共享 DN 数据与 GTM。

计算：目标吞吐乘本场景增长系数的规划年限次方。SQL QPS 除以该场景每事务 SQL 数；处理量除以有效窗口秒数。每生产 AZ 的 CN 数量为 max(2, ceil(目标吞吐 / 单核标定 / CPU水位 / 规划物理核))，再应用原取偶规则；手动数量保留，不足标红。自动最终规格重新核验吞吐，手动规格按实际核数/内存装箱，不偷偷提高。内存自动比例仍为工程假设，不是厂商保证。

测试示例：在线 16000 SQL QPS / 20 = 800 TPS，16C/64GB、50 TPS/核、70%水位，生产 AZ 各 2 CN；跑批 3600万业务单位 / 2小时 = 5000单位/秒，16C/96GB、100单位/秒/核、70%水位，关闭取偶时生产 AZ 各 5 CN。两生产中心各7 CN，灾备中心按现有比例取4 CN；18实例合计288物理核、1568GB。该例仅用于程序算式测试，不是银行系统标定推荐值。

共享 DN：未提供混合负载等效规划 TPS 时只按在线与数据量初算并持续标红；低于在线目标的混合输入不能降低在线资源，采用在线下限并提示复核。手动 DN 大于标定核数不外推性能，内存低于标定比例也提示未验证。不得把在线和跑批不同单位直接相加。

测试结果：
- `remediation-workloads.mjs`：21/21，包括三种跑批单位、手动数量/规格、无效参数、超大实例不强行落位、真实输入联动、四租户、生产反亲和、重置、390/875/1600宽度与真实下载。
- `remediation-primary-balance.mjs`：61/61，两模块与主分布相关回归。
- `remediation-b2-xml.mjs`：40/40，输出及字符边界回归。
- `remediation-reverse-quota.mjs`：64/64，反推配额与相关水位回归。
- 合计186项通过。语法检查及 diff 空白检查通过。
- 实际 XLSX 六张表、1587单元格与导出模型一致；ZIP/XML有效，无错误单元格或意外公式。原生 Excel/WPS 人工打开验收未完成。
- 已查看实际组网 PNG 与服务器 PNG，名称/规格分行，无观察到的遮挡；仅对测试规模负责，不宣称任意极大图均无问题。

证据：`../../outputs/goldendb-remediation-20260906/workloads/evidence/`。快照：`checkpoints/29-workloads-stage-verified.tar.gz`。未提交、未推送、未发布。

## 下一次续接 W06

### W06c1 执行计划（标定口径）

- [x] 在线/跑批分别支持“标定值待乘CPU水位”和“已含安全水位”两种口径；默认保持旧算法。
- [x] 已含安全水位时使用折减系数1，不解释为CPU利用率100%；隐藏旧水位草稿保留，切回后恢复校验。
- [x] 数量、自动规格、实际资源、公式、风险与导出使用同一有效折减系数；混合DN/GTM未评估红线不取消。
- [x] 等效标定一致性、模式切换、非法选项、默认兼容、输出和两模块回归；保存证据与快照。

本批不引入厂商性能承诺，不自动识别用户标定是否真实安全。跑批并行效率、重试及故障窗口留W06c2继续，R01–R18未关闭项保持。

W06c1结果：工作负载专项32/32，主分布/两模块61/61，手动规格适配9/9，合计102项通过；语法和diff检查通过。已含水位的70单位/秒/核与100×70%在数量、规格和能力上相同。选择已含安全水位但不改变输入100时，示例跑批从5个CN变为4个，这是用户改变标定解释后的结果，不是程序认定实际性能提高。需客户对输入依据负责，既有混合负载压测提示保留。

真实下载Excel1453单元格与模型一致（使用新口径后示例实例减少，表格长度不同），ZIP/XML与单元格有效；两类PNG下载通过。390/875/1600宽度无工作负载设置区横向溢出，875截图人工复核；原生Excel/WPS验收仍未完成。证据：`../../outputs/goldendb-remediation-20260906/w06c1/evidence/`，快照34（修改前）/35（验收）。本批与W06a/b均未提交/发布。

下一次从W06c2继续：先确定跑批并行效率、有效窗口和故障后剩余能力的输入口径与适用边界，再实现计算和验证；任务依赖、混合DN/GTM与版本证据不能凭空推导。完整待办见本文件W06c清单及总表R01–R18，不把W06c1完成等同整体生产认证。

### W06b 当前执行计划（2026-09-14）

- [x] 复现全手动CN规格被自动2GB/核前置适配拦截。
- [x] 只有全部租户/在线/跑批CN均手动时，绕过自动规格适配；任一自动场景仍保留原检查。
- [x] 保留实际实例资源、水位、反亲和和未落位红线；低规格仅边界用例，不作为产品最低规格。
- [x] 专项与已有回归、导出校验、结果存档；不重复W06a，不自动提交或发布。

W06b结果（2026-09-14）：客户CN机型4核/2GB、预留35%、手动CN实例1核/1GB的软件边界用例，修复前被自动2GB/核适配提前拦截；修复后交由真实实例需求装箱。以上不是可用于生产的产品规格建议。只要一个租户或跑批场景使用自动CN规格，仍执行旧前置适配；手动实例超过可用CPU/内存时不截小、不强行落位，保留红线。没有更改公式、装箱或反亲和算法。

验证：`remediation-w06b.mjs` 9/9；工作负载专项26/26；主分布/两模块回归61/61，合计96项。语法及diff检查通过；真实下载Excel1587单元格与模型一致，PNG下载回归通过。原生Excel/WPS人工验收仍未完成。证据 `../../outputs/goldendb-remediation-20260906/w06b/`，含before与evidence；修改前快照32，验收快照33。本批未提交或发布，W06a未提交修改一并保留。

### 下一次 W06c（待实施）

- [ ] 明确单核标定是否已包含CPU安全水位，避免重复折减；先记录口径及兼容策略。
- [ ] 评估跑批并行效率、有效窗口/重试及故障后剩余能力；不把线性初算写成完成时限承诺。
- [ ] 共享DN/GTM混合标定与在线/跑批重叠时段评估；没有数据时保持未评估提示。
- [ ] 更广的POC/混部/大规模导出及原生Office验收。W06总体与原R01–R18未关闭项继续保留。

### W06a 当前执行计划

- [x] 隔离拆分模式的隐藏旧 CN 手动数量；返回单场景时保留原值并恢复校验。
- [x] 分组手动数量标记由各场景实际输入决定，不引用旧 CN 标记。
- [x] 性能不足告警分别显示在线/跑批目标、能力及单位，不把总 CN 数套用在线 TPS。
- [x] 专项、两模块及导出回归；保存结果及快照。发布基线 a4ed9cf，本轮不提交或发布。

W06a验证：26/26工作负载专项（含新增5项边界）及61/61主分布/两模块回归，共87项通过。实际下载XLSX1587单元格一致；两类PNG实际下载成功，沿用原布局，875宽度截图复核。语法和diff检查通过。本批仅修复模式切换及告警口径，不改吞吐公式、实例资源、装箱或反亲和策略。证据另存 `../../outputs/goldendb-remediation-20260906/w06a/evidence/`，不覆盖W阶段旧证据。快照31。下一次继续W06b：手动小内存规格与前置自动适配边界；其后仍需并行效率、混合标定和故障窗口评估，不标记W06全部完成。

1. 当前跑批为线性初算，未建模并行效率、重试、任务依赖、N+1后的窗口完成时间。标定是否已经含水位也需要更明确输入口径。当前保留严格的共享 DN/GTM 压测红线，不能作为已通过生产验收方案。
2. 未实现在线/跑批重叠时段调度或混合负载标定数据导入，不能推导未知共享 DN/GTM 实际能力。
3. 继续审查手动小内存 CN 与旧全局自动适配前置检查、拆分模式隐藏旧 CN 手动值校验等边界；当前测试不能覆盖所有规格组合。
4. 补充原生 Excel/WPS、更多机型/POC混部与极大规模导出抽检。R01–R18原有未关闭事项保持不变。
