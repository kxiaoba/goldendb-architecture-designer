# S01b3b2b2b1 DN 数据倾斜校验

## 本批计划

- [x] 业务模块逐租户新增数据倾斜倍数，默认1，允许小数，独立于TPS热点倍数。
- [x] 最大分片数据 = 规划期租户数据 / Group数 × min(倾斜倍数, Group数)，不超过租户数据总量。
- [x] 与单主DN容量水位比较，联动红线、计算说明和Excel。
- [x] 默认保持旧均匀结果，不改自动分片、规格、节点、服务器装箱和反推模型。
- [x] 专项、回归、导出、布局验证与完成快照。

## 边界与续接

本批是敏感性校验，不代表真实逐Group采样。规划期数据已含既有增长规则，不再次乘增长或副本数。
TPS热点与数据倾斜是不同指标，不能互相替代；单分片场景不能凭倍数放大租户总数据。
整机磁盘仍使用既有均匀分布资源模型，未以每个Group同时最大容量的方式重复计费。
显示明确限制，不将单分片算式通过等同整机磁盘通过；实际分片容量、副本落位及RAID可用容量将在后续整合。
缺少业务吞吐的反推模式仍未评估本项，不伪造数据或TPS；用户新填数不构成压测证明。

后续：S01b3b2b2b2实际逐Group负载/容量与宿主机磁盘联动；IOPS/网络/回放和DN/GTM实测瓶颈；S01b3b2c同步仲裁及RPO/RTO证据；S02 RAID/三年磁盘容量；S03行业回归和原生Office导出验收。既有R01-R18遗留项不关闭。

存档：`../../outputs/goldendb-spec-remediation-20260919/`，本批专项子目录 `s01b3b2b2b1/`。
当前仅本地修改，不提交、不推送、不发布。

## 2026-09-19 验收记录

- 专项24/24，热点24/24、宿主机压力19/19、单副本21/21、两模块主分布61/61、在线跑批45/45，共194项通过。
- Excel实际下载1652单元格与模型一致，ZIP/XML通过；未运行原生Excel/WPS视觉验收。
- 390/875/1600输入布局检查通过，已查看390截图，无新增输入遮挡；邻接回归包含两类PNG和Excel。
- 语法、diff空白检查通过，浏览器异常0。新专项初次执行即24项通过。
- 3TB、4Group、倾斜倍数3时，平均0.75TB，最大分片2.25TB，超过2TB水位；原服务器落位保持，风险联动。
- 快照：`before-s01b3b2b2b1.tar.gz`、`after-s01b3b2b2b1.tar.gz`；邻接回归目录 `s01b3b2b2b1-regression/`。

后续S01b3b2b2b2已完成逐Group录入数据和磁盘联动，详见 `remediation-dn-group-data.md`。下次继续逐Group性能及实测瓶颈证据，不重做输入。不以本批通过关闭IO网络、同步容灾或S02/S03等遗留项。
