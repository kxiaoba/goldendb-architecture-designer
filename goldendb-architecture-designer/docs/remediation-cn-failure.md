# S01b3a CN 实际容量与单机故障检查

日期：2026-09-17。用户授权继续遗留修改。本批接续 S01b2b，本地保存，不提交发布。

## 已实施

- [x] 单场景、拆分在线、未启用窗口审计的跑批，逐租户/场景/AZ检查实际有效容量。
- [x] 只计入实际落位且CPU/内存/磁盘水位通过的物理机上的CN，重复角色不重复计数。
- [x] 正常能力=有效实例数×单实例安全吞吐；故障能力=(有效实例数-本场景CN最多的一台服务器的实例数)×单实例安全吞吐。
- [x] 各生产AZ独立对比完整需求，不汇总两个AZ后判通过，不自动变更手工数量或规格。
- [x] 生产中心正常不足进入红线；生产环境单机故障不足也进入红线。POC单机故障不足仅提示。
- [x] 异地中心显示全量接管参考，不擅自增加生产红线或假定容灾比例就是业务接管目标。
- [x] 计算过程下方节点审计、风险列表、资源红线和Excel摘要联动；拓扑图不增加长说明。
- [x] 已启用窗口的跑批继续走原时长/物理机故障审计，避免以平均吞吐替代窗口判断。
- [x] 反推模块缺少工作负载TPS，不新增虚构的性能判断。

未改动实例规划、装箱、主分布、DN/GTM/管理节点及导出布局。旧参数和上批未提交修改保留。

## 独立复算与测试

案例：在线16000QPS、T=20，完整目标800TPS/AZ。16C×50TPS/核×0.7=560TPS/CN。
每生产AZ两台各承载一CN：正常1120TPS，单机故障560TPS，新增故障不足红线。
手工增至三CN并分别落位：正常1680TPS，故障1120TPS，容量算式满足；不代表实测性能认证。
两CN共一物理机时故障损失按两个实例计算，而不是只减一个。移除实际角色或水位不通过后不再计入能力。

专项 `tests/remediation-cn-failure.mjs` 25/25，包括上述复算、POC/生产差异、DR、失落位、重复角色、跨租户、旧单场景、只读不改模型、下载与390/875/1600布局。
原 `remediation-workloads.mjs` 45/45；`remediation-primary-balance.mjs` 61/61。
Excel实际下载1606单元格对比无差异，ZIP/XML完整，无公式/错误单元格。手机截图已查看，无文字遮挡。
上批输入专项 `remediation-cn-inputs.mjs` 33/33；总164项检查通过。语法及diff空白检查通过。
未进行数据库压测或原生Excel/WPS客户端视觉验收。

## 存档与续接

目录：`../../outputs/goldendb-spec-remediation-20260917/`。
修改前/后快照：`before-s01b3a.tar.gz`、`after-s01b3a.tar.gz`。
证据：`s01b3a/results.json`、截图/PNG/XLSX、`workbook-validation.json`、`workloads/`、`balance/`、`inputs/`。

下一批 S01b3b：先分析DN每Group副本可提升位置、单机/中心故障、共享负载，再实现接管检查；不能直接把全部从副本TPS相加。
S01b2b完整证据状态、S02 RAID/三年增长/日志临时空间/磁盘80%红线、S03三行业生产POC联调仍未关闭。
单机故障审计不等于机架故障、整个AZ失效、切换RTO、GTM仲裁或共享DN瓶颈已验证。
