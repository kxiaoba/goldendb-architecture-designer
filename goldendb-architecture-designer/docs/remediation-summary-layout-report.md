# 节点分布说明位置调整

2026-09-10，接续主分片均衡专项。用户反馈组网图上方长说明影响阅读，本次仅调整展示位置。

- 删除两类图内的 `renderCnPlacementSummary` 调用，保留图中的服务器、组件、M/S 角色与关系。
- 在“计算过程”后新增“节点落位与主分布”，统一渲染原说明，不丢失逐中心 CN/主机数和 DN 主从统计。
- 两模块切换与参数变化实时更新；无效参数时清除旧统计，恢复合法输入后重新显示。
- PNG 只导出图本身，不含移动后的长说明；Excel 和复制摘要仍保留完整统计。
- 未修改计算、分配、均衡策略、规格或红线函数。

验证：61/61 主均衡与位置专项、40/40 输出回归。两模块、三种主中心策略、POC/生产、三种多中心部署、手调/混部、多租户、390/875/1600宽度、两类PNG与Excel下载检查通过。真实 Excel 2875 单元格核对无差异；原生Excel/WPS客户端未验收。实际组网PNG已查看，长说明已移除。

证据 `../../outputs/goldendb-remediation-20260906/mp-layout/evidence/`。
修改前快照 `checkpoints/22-before-summary-move.tar.gz`，完成快照 `checkpoints/23-summary-moved.tar.gz`。
本次未提交、未推送、未发布。下一步仍为 B4b 剩余中心配额，不重复调整此布局。
